package com.wangweimin.learnspark

import scala.util.Random

object SampleDataFileGenerator {
  def main(args: Array[String]){
   /* val writer = new FileWriter(new File("C: \\bigdata\\sample_age_data.txt"),false)
    val rand = new Random()
    for ( i <- 1 to 1000000) {
      writer.write( i + " " + rand.nextInt(100))
      writer.write(System.getProperty("line.separator"))
    }
    writer.flush()
    writer.close()*/
  }
}
